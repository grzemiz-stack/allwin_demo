# Vapi Web SDK — tryb LIVE (faza 2). Tryb skryptowy działa bez tych zmiennych.
# Publiczny klucz Vapi (bezpieczny po stronie klienta).
NEXT_PUBLIC_VAPI_PUBLIC_KEY=
# ID osobnego asystenta DEMO (izolowany — tool-calle lecą na /api/demo/tool,
# który zwraca spreparowane sukcesy; żadnego realnego dispatchu).
NEXT_PUBLIC_VAPI_DEMO_ASSISTANT_ID=
